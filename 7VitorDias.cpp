#include <stdio.h>
#include <locale.h>

/*int main() {

    setlocale(0, "Portuguese");

  int num[10];
  int i, count, searchNum;

  printf("Digite 10 números(espaçados):\n");
  for (i = 0; i < 10; i++) {
    scanf("%d", &num[i]);
  }

  count = 0;
  for (i = 0; i < 10; i++) {
    if (num[i] == 3) {
      count++;
    }
  }
  printf("O número 3 aparece %d vezes no vetor.\n", count);

  printf("Digite um número para pesquisar no vetor: ");
  scanf("%d", &searchNum);


  count = 0;
  for (i = 0; i < 10; i++) {
    if (num[i] == searchNum) {
      count++;
    }
  }
  printf("O número %d aparece %d vezes no vetor.\n", searchNum, count);

  return 0;
}
*/

/*int main() {

    setlocale(0, "Portuguese");

    int nums[10];
    int i, count, searchNum = 0;

    
    printf("Digite 10 números:\n");
    for (i = 0; i < 10; i++) {
        scanf("%d", &nums[i]);
    }

    printf("Digite um número para pesquisar no vetor: ");
    scanf("%d", &searchNum);

    for (i = 0; i < 10; i++) {
        if (nums[i] == searchNum) {
            count++;
        }
    }

    printf("O número %d aparece %d vezes no vetor.\n", searchNum, count);

    return 0;
}
*/
